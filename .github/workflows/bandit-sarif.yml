name: Bandit SARIF

on:
  push:
    branches: [main]
  pull_request:
    branches: [dev]
  schedule:
    - cron: "27 01 * * *"
  workflow_dispatch: {}

permissions:
  contents: read
  security-events: write
  actions: read

concurrency:
  group: bandit-sarif-${{ github.ref }}
  cancel-in-progress: true

jobs:
  bandit-security-scan:
    name: Bandit Security Scan (SARIF)
    runs-on: ubuntu-latest
    env:
      BANDIT_VERSION: "1.9.3"

    steps:
      - # Checkout repository code
        name: Checkout
        uses: actions/checkout@v6

      - # Load central tool-version pins, overriding the env: fallbacks above.
        name: Load tool versions
        run: |
          if [ -f .development/versions.env ]; then
            grep -vE '^\s*(#|$)' .development/versions.env >> "$GITHUB_ENV"
          fi

      - # Setup Python for Bandit scanning
        name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - # Install Bandit and SARIF formatter
        name: Install tools
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install "bandit==${{ env.BANDIT_VERSION }}" bandit-sarif-formatter

      - # Execute Bandit and emit SARIF
        name: Run Bandit (SARIF)
        run: |
          mkdir -p reports
          bandit -r octoprint_pandabreath -x .venv,venv,.git -ll \
            --format sarif --output reports/bandit.sarif --exit-zero

      - # Upload SARIF to GitHub Security tab
        name: Upload Bandit SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: reports/bandit.sarif
